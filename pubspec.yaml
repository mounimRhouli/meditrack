name: meditrack
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.2
  provider: ^6.1.1
  path: ^1.9.0
  path_provider: ^2.1.2 # Critical for Phase 3 local storage
  flutter_riverpod: ^2.4.9
  cupertino_icons: ^1.0.8
  
  # --- HARMONIZED FIREBASE STACK (FIXED) ---
  # These versions are guaranteed to solve together
  firebase_core: ^3.10.0
  firebase_auth: ^5.4.1
  cloud_firestore: ^5.6.1
  firebase_storage: ^12.4.0 # Upgraded to match Core 3.x
  
  # --- NAVIGATION & UTILS ---
  go_router: ^14.0.0
  intl: ^0.19.0
  uuid: ^4.3.3
  dartz: ^0.10.1
  equatable: ^2.0.5
  connectivity_plus: ^6.0.0

  # --- NOTIFICATIONS (Phase 2) ---
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.2

  # --- DOCUMENTS & OCR (Phase 3) ---
  image_picker: ^1.1.2
  google_mlkit_text_recognition: ^0.13.0

  url_launcher: ^6.1.11
  fl_chart: ^0.63.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true